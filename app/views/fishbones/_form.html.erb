<%= form_for(@fishbone) do |f| %>
  <% if @fishbone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fishbone.errors.count, "error") %> prohibited this fishbone from being saved:</h2>

      <ul>
      <% @fishbone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grupo %><br>
    <%= f.text_field :grupo %>
  </div>
  <div class="field">
    <%= f.label :causa %><br>
    <%= f.text_field :causa %>
  </div>
  <div class="field">
    <%= f.label :relevancia %><br>
    <%= f.check_box :relevancia %>
  </div>
  <div class="field">
    <%= f.label :acao %><br>
    <%= f.text_area :acao %>
  </div>
  <div class="field">
    <%= f.label :responsavel_id %><br>
    <%= f.collection_select(:responsavel_id, Responsavel.all, :id, :nome, {:prompt => 'Selecione o Responsável'} ) %>
  </div>
  <div class="field">
    <%= f.label :prazo %><br>
    <%= f.date_select :prazo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
